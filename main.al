let abc = [1,2,3,4];
println("passss");
let xyz = abc[0];
println(inttostr(xyz));

let max = array_length(abc);
let start = 0;
for (start = 0; start < max; start = start + 1) {
    println(inttostr(abc[start]));
}
